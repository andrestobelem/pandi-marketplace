{
  "name": "pandi-launchpad-ts",
  "version": "0.1.0",
  "description": "TypeScript, MCP-less bridge between a Novation Launchpad X and Claude Code, exposed as a skill instead of an MCP tool.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@julusian/midi": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
